	.data
buf:	.space	64
format: .asciz "%d\n"
counter: .long 0
	.text
	.global	main
main:
	pushq	$0
	call 	inimage
inImage:
	movq	$buf, %rdi
	movq	$5, %rsi
	movq	stdin, %rdx
	call	fgets
getInt:
	movq	$buf,%rdi
	pushq	%counter
	call	checkifInt
checkifInt:
	popq	%rsi
	movq	(%rdi,,%rsi),%rdi
	cmpq	$32, %rdi
	je		continueloop
	cmpq	$43, %rdi
	je		continueloop
	cmpq	$45, %rdi
	je		continueloop
	cmpq	$48, %rdi
	jle		checkifnumber
	call 	endloop
checkifnumber:
	cmpq	$57, %rdi
	jle		continueloop
continueloop:
	pushq	%rdi
	incq	%counter
	pushq	%counter
	call	checkifInt
endloop:

